name: Integracion y entrega continua
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@master
      name: copiar repositorio con scp
      uses: appleboy/scp-action@master
